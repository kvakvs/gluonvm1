# Gluon System Library

System abstraction layer for GluonVM, covers all OS interfacing needs of a VM,
such as files, memory, network etc.

Implement same API as seen in g_sys.h to support a new system.
